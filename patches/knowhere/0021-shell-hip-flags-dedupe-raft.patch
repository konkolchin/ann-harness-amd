diff --git a/cmake/libs/knowhere_hip_host_fixup.cmake b/tmp/kh-p21work.cmake
index 9156d97..1a5c8a3 100644
--- a/cmake/libs/knowhere_hip_host_fixup.cmake
+++ b/cmake/libs/knowhere_hip_host_fixup.cmake
@@ -12,7 +12,7 @@ function(knowhere_strip_offload_opts _in _out_var)
       set(_skip_next TRUE)
       continue()
     endif()
-    if(_item MATCHES "offload-arch" OR _item STREQUAL "-xhip" OR _item STREQUAL "hip")
+    if(_item MATCHES "offload-arch" OR _item MATCHES "cuda-gpu-arch" OR _item STREQUAL "-xhip" OR _item STREQUAL "hip")
       continue()
     endif()
     list(APPEND _out "${_item}")
@@ -65,8 +65,15 @@ if(TARGET knowhere AND _knowhere_fixup_rocm)
   if(DEFINED ENV{HIP_ARCH})
     set(_knowhere_gfx "$ENV{HIP_ARCH}")
   endif()
-  target_compile_options(knowhere PRIVATE "-x" "hip" "--offload-arch=${_knowhere_gfx}")
-  message(STATUS "KNOWHERE WITH_HIP: knowhere via clang++ -x hip --offload-arch=${_knowhere_gfx}")
+  foreach(_prop COMPILE_OPTIONS INTERFACE_COMPILE_OPTIONS)
+    get_target_property(_opts knowhere ${_prop})
+    if(_opts)
+      knowhere_strip_offload_opts(_opts _opts_clean)
+      set_target_properties(knowhere PROPERTIES ${_prop} "${_opts_clean}")
+    endif()
+  endforeach()
+  target_compile_options(knowhere PRIVATE "SHELL:-x hip --offload-arch=${_knowhere_gfx}")
+  message(STATUS "KNOWHERE WITH_HIP: knowhere SHELL:-x hip --offload-arch=${_knowhere_gfx}")
   unset(_knowhere_gfx)
 endif()
 unset(_knowhere_fixup_rocm)
