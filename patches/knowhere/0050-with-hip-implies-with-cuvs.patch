diff --git a/CMakeLists.txt b/CMakeLists.txt
index ac237ca..b0c1d2e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -52,9 +52,11 @@ include(cmake/utils/platform_check.cmake)
 knowhere_option(WITH_CUVS "Build with CUVS indexes" OFF)
 knowhere_option(WITH_HIP "Link prebuilt hipVS/hipRAFT (ROCm) instead of CUDA cuVS" OFF)
 message(STATUS "KNOWHERE: WITH_CUVS=${WITH_CUVS} WITH_HIP=${WITH_HIP}")
 if(WITH_HIP AND NOT WITH_CUVS)
-  message(FATAL_ERROR
-    "WITH_HIP=ON but WITH_CUVS=OFF. Re-run: conan install .. -o with_cuvs=True ; cmake -DWITH_CUVS=ON")
+  # Nested Milvus configures can re-enter Knowhere with a stale/empty WITH_CUVS
+  # cache even when the parent passed -DWITH_CUVS=ON. HIP always needs cuVS/hipVS.
+  set(WITH_CUVS ON CACHE BOOL "Build with CUVS indexes" FORCE)
+  message(STATUS "KNOWHERE: WITH_HIP=ON -> forcing WITH_CUVS=ON")
 endif()
 if(WITH_CUVS AND WITH_HIP)
   include(cmake/libs/libhipcuvs.cmake)
