diff --git a/CMakeLists.txt b/CMakeLists.txt
index e4ba04a..ea5b426 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -278,36 +278,8 @@ if(WITH_CUVS AND WITH_HIP)
     endif()
   endforeach()
   target_link_libraries(knowhere PUBLIC raft::raft cuvs::cuvs rmm::rmm)
-  foreach(_rmm_dep rmm::rmm_logger_impl rmm::rmm_logger rapids_logger::rapids_logger)
-    if(TARGET ${_rmm_dep})
-      target_link_libraries(knowhere PUBLIC ${_rmm_dep})
-      message(STATUS "KNOWHERE WITH_HIP: linked ${_rmm_dep}")
-    endif()
-  endforeach()
-  if(DEFINED ENV{INSTALL_PREFIX})
-    set(_knowhere_install_prefix "$ENV{INSTALL_PREFIX}")
-    if(_knowhere_install_prefix MATCHES "^~")
-      if(DEFINED ENV{HOME})
-        string(REPLACE "~" "$ENV{HOME}" _knowhere_install_prefix "${_knowhere_install_prefix}")
-      endif()
-    endif()
-    find_library(_knowhere_librmm NAMES rmm PATHS "${_knowhere_install_prefix}/lib"
-                 NO_DEFAULT_PATH)
-    if(_knowhere_librmm)
-      target_link_libraries(knowhere PUBLIC "${_knowhere_librmm}")
-      message(STATUS "KNOWHERE WITH_HIP: linked ${_knowhere_librmm}")
-    endif()
-    find_library(_knowhere_librapids_logger NAMES rapids_logger PATHS
-                 "${_knowhere_install_prefix}/lib" NO_DEFAULT_PATH)
-    if(_knowhere_librapids_logger)
-      target_link_libraries(knowhere PUBLIC "${_knowhere_librapids_logger}")
-      message(STATUS "KNOWHERE WITH_HIP: linked ${_knowhere_librapids_logger}")
-    endif()
-    unset(_knowhere_install_prefix)
-  endif()
-  unset(_rmm_dep)
-  unset(_knowhere_librmm)
-  unset(_knowhere_librapids_logger)
+  include(cmake/libs/knowhere_hip_link.cmake)
+  knowhere_hip_link_rmm(knowhere)
 endif()
 unset(_hip_dep)
 unset(_hip_inc)
diff --git a/cmake/libs/knowhere_hip_host_fixup.cmake b/cmake/libs/knowhere_hip_host_fixup.cmake
index 975620f..87cc4bb 100644
--- a/cmake/libs/knowhere_hip_host_fixup.cmake
+++ b/cmake/libs/knowhere_hip_host_fixup.cmake
@@ -86,11 +86,12 @@ if(TARGET knowhere AND _knowhere_fixup_rocm)
     endif()
   endforeach()
   target_compile_options(knowhere PRIVATE "SHELL:-x hip --offload-arch=${_knowhere_gfx}")
+  target_link_options(knowhere PRIVATE "LINKER:--hip-link")
   message(STATUS "KNOWHERE WITH_HIP: knowhere SHELL:-x hip --offload-arch=${_knowhere_gfx}")
 
   knowhere_file_glob(GLOB_RECURSE _knowhere_cuvs_cu src/common/cuvs/*.cu)
   if(_knowhere_cuvs_cu)
-    add_library(knowhere_cuvs_hip STATIC ${_knowhere_cuvs_cu})
+    add_library(knowhere_cuvs_hip OBJECT ${_knowhere_cuvs_cu})
     set_target_properties(knowhere_cuvs_hip PROPERTIES POSITION_INDEPENDENT_CODE ON)
     target_compile_definitions(knowhere_cuvs_hip PRIVATE
       KNOWHERE_WITH_CUVS
@@ -114,16 +115,12 @@ if(TARGET knowhere AND _knowhere_fixup_rocm)
         target_link_libraries(knowhere_cuvs_hip PUBLIC ${_hip_dep})
       endif()
     endforeach()
-    foreach(_rmm_dep rmm::rmm_logger_impl rmm::rmm_logger rapids_logger::rapids_logger)
-      if(TARGET ${_rmm_dep})
-        target_link_libraries(knowhere_cuvs_hip PUBLIC ${_rmm_dep})
-      endif()
-    endforeach()
+    include(cmake/libs/knowhere_hip_link.cmake)
+    knowhere_hip_link_rmm(knowhere_cuvs_hip)
     target_compile_options(knowhere_cuvs_hip PRIVATE
       "SHELL:-x hip --offload-arch=${_knowhere_gfx}")
-    target_link_libraries(knowhere PRIVATE
-      "$<LINK_LIBRARY:WHOLE_ARCHIVE,knowhere_cuvs_hip>")
-    message(STATUS "KNOWHERE WITH_HIP: knowhere_cuvs_hip WHOLE_ARCHIVE (${_knowhere_cuvs_cu})")
+    target_sources(knowhere PRIVATE $<TARGET_OBJECTS:knowhere_cuvs_hip>)
+    message(STATUS "KNOWHERE WITH_HIP: knowhere_cuvs_hip OBJECT -> knowhere (${_knowhere_cuvs_cu})")
   endif()
   unset(_knowhere_cuvs_cu)
   unset(_hip_dep)
diff --git a/cmake/libs/knowhere_hip_link.cmake b/cmake/libs/knowhere_hip_link.cmake
new file mode 100644
index 0000000..5543557
--- /dev/null
+++ b/cmake/libs/knowhere_hip_link.cmake
@@ -0,0 +1,97 @@
+# Resolve RMM / rapids-logger symbols inside libknowhere.so (HIP cuVS path).
+function(knowhere_hip_link_rmm _target)
+  set(_knowhere_has_logger_target FALSE)
+  foreach(_rmm_dep rmm::rmm_logger_impl rmm::rmm_logger rapids_logger::rapids_logger)
+    if(TARGET ${_rmm_dep})
+      target_link_libraries(${_target} PUBLIC ${_rmm_dep})
+      message(STATUS "KNOWHERE WITH_HIP: ${_target} linked ${_rmm_dep}")
+      if(_rmm_dep MATCHES "logger")
+        set(_knowhere_has_logger_target TRUE)
+      endif()
+    endif()
+  endforeach()
+
+  set(_knowhere_prefixes ${CMAKE_PREFIX_PATH})
+  if(DEFINED ENV{INSTALL_PREFIX})
+    set(_knowhere_install_prefix "$ENV{INSTALL_PREFIX}")
+    if(_knowhere_install_prefix MATCHES "^~")
+      if(DEFINED ENV{HOME})
+        string(REPLACE "~" "$ENV{HOME}" _knowhere_install_prefix "${_knowhere_install_prefix}")
+      endif()
+    endif()
+    get_filename_component(_knowhere_install_prefix "${_knowhere_install_prefix}" ABSOLUTE)
+    list(PREPEND _knowhere_prefixes "${_knowhere_install_prefix}")
+    unset(_knowhere_install_prefix)
+  endif()
+  list(REMOVE_DUPLICATES _knowhere_prefixes)
+
+  set(_knowhere_librmm "")
+  set(_knowhere_librapids_logger "")
+  foreach(_prefix IN LISTS _knowhere_prefixes)
+    if(NOT _knowhere_librmm)
+      find_library(_knowhere_librmm NAMES rmm PATHS "${_prefix}/lib" NO_DEFAULT_PATH)
+    endif()
+    if(NOT _knowhere_librapids_logger)
+      find_library(_knowhere_librapids_logger NAMES rapids_logger PATHS "${_prefix}/lib"
+                   NO_DEFAULT_PATH)
+    endif()
+  endforeach()
+  if(_knowhere_librmm)
+    target_link_libraries(${_target} PUBLIC "-Wl,--push-state" "-Wl,--no-as-needed"
+                          "${_knowhere_librmm}" "-Wl,--pop-state")
+    message(STATUS "KNOWHERE WITH_HIP: ${_target} linked ${_knowhere_librmm}")
+  endif()
+  if(_knowhere_librapids_logger)
+    target_link_libraries(${_target} PUBLIC "-Wl,--push-state" "-Wl,--no-as-needed"
+                          "${_knowhere_librapids_logger}" "-Wl,--pop-state")
+    message(STATUS "KNOWHERE WITH_HIP: ${_target} linked ${_knowhere_librapids_logger}")
+  endif()
+
+  if(_knowhere_has_logger_target)
+    unset(_knowhere_has_logger_target)
+    unset(_knowhere_prefixes)
+    unset(_knowhere_librmm)
+    unset(_knowhere_librapids_logger)
+    unset(_prefix)
+    unset(_candidate)
+    unset(_rmm_dep)
+    return()
+  endif()
+
+  set(_knowhere_logger_cpp "")
+  foreach(_prefix IN LISTS _knowhere_prefixes)
+    foreach(_candidate IN ITEMS
+        "include/rmm/logger_impl/logger.cpp"
+        "include/rmm/detail/logger_impl/logger.cpp"
+        "include/rapids_logger/logger_impl/logger.cpp")
+      if(EXISTS "${_prefix}/${_candidate}")
+        set(_knowhere_logger_cpp "${_prefix}/${_candidate}")
+        break()
+      endif()
+    endforeach()
+    if(_knowhere_logger_cpp)
+      break()
+    endif()
+  endforeach()
+  if(_knowhere_logger_cpp)
+    target_sources(${_target} PRIVATE "${_knowhere_logger_cpp}")
+    message(STATUS "KNOWHERE WITH_HIP: ${_target} compiles rmm logger from ${_knowhere_logger_cpp}")
+    if(TARGET spdlog::spdlog)
+      target_link_libraries(${_target} PUBLIC spdlog::spdlog)
+    elseif(TARGET spdlog::spdlog_header_only)
+      target_link_libraries(${_target} PUBLIC spdlog::spdlog_header_only)
+    endif()
+  else()
+    message(WARNING
+      "KNOWHERE WITH_HIP: rmm logger impl not found; libknowhere may have undefined rmm::logger symbols")
+  endif()
+
+  unset(_knowhere_has_logger_target)
+  unset(_knowhere_prefixes)
+  unset(_knowhere_librmm)
+  unset(_knowhere_librapids_logger)
+  unset(_knowhere_logger_cpp)
+  unset(_prefix)
+  unset(_candidate)
+  unset(_rmm_dep)
+endfunction()
